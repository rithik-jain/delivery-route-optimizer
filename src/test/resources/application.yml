server:
  port: 0

app:
  routing:
    exact-threshold: 7
    average-speed-kmph: 20.0

logging:
  level:
    com.lucidity: WARN
    root: WARN
  pattern:
    console: "%d{HH:mm:ss.SSS} [%X{requestId:-NO_REQ}] %-5level %logger{36} - %msg%n"
