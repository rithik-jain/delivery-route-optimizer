server:
  port: 8080

spring:
  application:
    name: delivery-route-optimizer

app:
  routing:
    exact-threshold: 7
    average-speed-kmph: 20.0

logging:
  level:
    com.lucidity: DEBUG
    root: INFO
